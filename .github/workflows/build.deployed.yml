name: Build and deply code

#to define whtn the code should be triggered we define the on command as below 
on: [push,pull_request]

jobs:
  job1:
    env:
      DATABASE_HOSTNAME: localhost
      DATABASE_PORT: 5432
      DATABASE_PASSWORD: vivek123
      DATABASE_NAME: FastApi
      DATABASE_USERNAME: postgres
      SECRET_KEY: sdgasdg83223asdfa9sdg3dsga1231ddgds
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v4
      - name: installing python 
        uses: actions/setup-python@v5
        with: 
          python-version: '3.10'
      - name: update pip
        run: python -m pip install --upgrade pip
      - name: installing all dependences
        run: pip install -r requirements.txt  
      - name: adding pytest workflow 
        run: |
          pip install pytest
          pytest 